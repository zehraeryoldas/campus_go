import 'package:flutter/cupertino.dart';

class favorilerim extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Deneme Metin2"),
      ),
    );
  }
}